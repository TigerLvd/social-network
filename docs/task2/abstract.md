Доработай функционал по работе с друзьями по описанию из com.highload.architect.soc.network.api.DialogApi - в com.highload.architect.soc.network.api.DialogApiImpl доработай функционал в методах.

Работу необходимо выполнять итерационно, по TDD:
Добавление тестов и реализации необходимо по принципу red-gren-refactoring:
- в начале пишем тест по планируемому функционалу, запускаем - он падает, т.к. функционала ещё нет - добавляем функционал и снова проверяем тесты, правим пока не тесты не пройдут. Далее делаем рефакторинг. Проверяем тесты, правим пока не тесты не пройдут.

Нужно добавить тесты на проверку успешных сценариев (что метод отрабатывает с 200, что возвращается сообщение, ...).

Работу необходимо выполнять итерационно: в начале нужно добавить реализацию без БД (хранить события в коллекции, в методе), далее добавить модель, мапинг в DTO (если надо), сервисы и dao.